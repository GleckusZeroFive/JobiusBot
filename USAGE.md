# Как работать с ботом

## Управление ботом

Все скрипты теперь можно запускать **из корня проекта**:

```bash
./start.sh      # Запустить бота
./stop.sh       # Остановить бота
./restart.sh    # Перезапустить бота
./status.sh     # Проверить статус
./logs.sh       # Посмотреть логи
```

### Примеры использования логов

```bash
./logs.sh           # Последние 50 строк
./logs.sh 100       # Последние 100 строк
./logs.sh -f        # Отслеживание в реальном времени (Ctrl+C для выхода)
./logs.sh -e        # Только ошибки
```

## Команды бота

### Список команд для пользователей

- `/start` — Запустить бота и показать главное меню
- `/search` — Поиск вакансий (понимает естественный язык!)
- `/help` — Справка по использованию
- `/stats` — Показать мою статистику
- `/calc` — Калькулятор
- `/favorites` — Избранные вакансии

### Главное меню (4 кнопки)

```
[🔍 Поиск работы] [⭐ Избранное]
[🔢 Калькулятор]  [📊 Статистика]
```

## Примеры запросов для поиска

Бот понимает естественный язык! Пишите как удобно:

**Структурированные запросы:**
- `Python developer`
- `Python junior Москва`
- `Backend middle СПб 200000`

**Естественный язык:**
- `Хочу удаленную работу python от 150к`
- `Ищу начальную позицию в IT с гибким графиком`
- `Frontend разработчик удаленка`

**Как работает:**
1. Сначала бот пробует **LLM-парсинг** (умный режим)
2. Если LLM недоступен → автоматический **fallback на regex**
3. Базовый поиск **всегда работает**!

## Структура проекта

```
JobiusBot/
├── bot.py              # Главный файл бота
├── config.py           # Конфигурация
├── hh_api.py           # API HeadHunter
├── requirements.txt    # Зависимости
├── .env                # Токены и настройки (не в git)
├── handlers/           # Обработчики команд
├── keyboards/          # Клавиатуры бота
├── utils/              # Утилиты (LLM, pagination, states)
├── database/           # База данных
├── middlewares/        # Middleware (LLM классификация)
├── scripts/            # Скрипты управления ботом
├── docs/               # Документация
├── logs/               # Логи бота
└── venv/               # Виртуальное окружение
```

## Настройка бота

### 1. Установка зависимостей

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните `.env`:

```env
# Telegram Bot Token от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Groq API Keys (минимум 1, максимум 4)
GROQ_API_KEY_1=your_groq_key_1
GROQ_API_KEY_2=your_groq_key_2
GROQ_API_KEY_3=your_groq_key_3
GROQ_API_KEY_4=your_groq_key_4

# Модель LLM (по умолчанию llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile

# База данных (по умолчанию jobius.db)
DATABASE_PATH=jobius.db

# Логи (по умолчанию INFO)
LOG_LEVEL=INFO
```

### 3. Запуск

```bash
./start.sh
```

## Troubleshooting

### Бот не запускается

1. Проверьте логи:
   ```bash
   ./logs.sh -e
   ```

2. Проверьте `.env` файл (токены заполнены?)

3. Проверьте venv:
   ```bash
   source venv/bin/activate
   python bot.py
   ```

### LLM не работает

1. Проверьте Groq API ключи в `.env`
2. Убедитесь, что хотя бы один ключ валиден
3. Даже если LLM недоступен, **базовый поиск работает** (fallback на regex)

### Ошибки импорта

```bash
source venv/bin/activate
pip install -r requirements.txt --upgrade
```

## Разработка

### Тестирование изменений

1. Остановите бота: `./stop.sh`
2. Внесите изменения
3. Запустите: `./start.sh`
4. Проверьте логи: `./logs.sh -f`

### Проверка синтаксиса

```bash
python3 -m py_compile handlers/*.py
```

### Проверка импортов

```bash
./venv/bin/python -c "import handlers.search; import handlers.basic; print('OK')"
```

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Aiogram Documentation](https://docs.aiogram.dev/)
- [HeadHunter API](https://dev.hh.ru/)
- [Groq API](https://console.groq.com/)
