# Настройка LLM интеграции (Groq API)

## Обзор

JobiusBot использует AI-ассистента на базе **Groq API** для анализа сообщений пользователей и помощи в фокусировании на поиске работы.

### Используемая модель:
- **llama-3.3-70b-versatile** (рекомендуется)
- Альтернатива: llama-3.1-8b-instant (быстрее, но менее точная)

## Получение API ключей

### Шаг 1: Регистрация на Groq

1. Перейдите на [console.groq.com](https://console.groq.com)
2. Нажмите "Sign Up" или войдите через Google/GitHub
3. Подтвердите email (если требуется)

### Шаг 2: Создание API ключей

1. В консоли перейдите в раздел **API Keys**
2. Нажмите **Create API Key**
3. Задайте имя ключу (например, `JobiusBot-Key-1`)
4. Скопируйте и сохраните ключ в безопасном месте
5. **Повторите для создания 2-4 ключей** (для ротации)

### Бесплатный план Groq:

- ✅ До 14,400 запросов в день на llama-3.3-70b
- ✅ До 30,000 запросов в день на llama-3.1-8b
- ✅ Без привязки карты
- ℹ️ Лимиты обновляются ежедневно

## Настройка в проекте

### 1. Откройте файл `.env`

```bash
nano .env  # или используйте любой редактор
```

### 2. Добавьте ваши API ключи

```env
# Groq API Keys (можно указать от 1 до 4 ключей)
GROQ_API_KEY_1=gsk_ваш_первый_ключ_здесь
GROQ_API_KEY_2=gsk_ваш_второй_ключ_здесь
GROQ_API_KEY_3=gsk_ваш_третий_ключ_здесь
GROQ_API_KEY_4=gsk_ваш_четвертый_ключ_здесь

# Модель (опционально, по умолчанию llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile
```

### 3. Сохраните и перезапустите бота

```bash
./restart.sh
```

## Как работает ротация ключей

Бот использует алгоритм **round-robin**:

1. Запрос 1 → Ключ 1
2. Запрос 2 → Ключ 2
3. Запрос 3 → Ключ 3
4. Запрос 4 → Ключ 4
5. Запрос 5 → Ключ 1 (цикл повторяется)

Это позволяет:
- ⚡ Распределить нагрузку между ключами
- 🔄 Избежать превышения лимитов
- 🛡️ Обеспечить отказоустойчивость

## Функционал LLM ассистента

### 1. Классификация сообщений

Бот анализирует каждое сообщение и определяет его категорию:

- **job_search** - поиск работы, вакансии, резюме
- **bot_help** - вопросы о боте, команды
- **calculator** - математика
- **offtopic** - не по теме работы

### 2. Отслеживание offtopic

Бот ведёт статистику для каждого пользователя:

- **consecutive_offtopic** - последовательные сообщения не по теме
- **offtopic_count** - общее количество offtopic сообщений

### 3. Правило сброса сессии

Сессия пользователя **сбрасывается** при условии:

```
consecutive_offtopic >= 3  И  offtopic_count >= 10
```

То есть:
- Минимум 3 сообщения не по теме **подряд**
- **И** минимум 10 сообщений не по теме **всего**

При сбросе:
- ❌ Очищается история диалога
- ❌ Обнуляются счётчики offtopic
- 🔄 Бот возвращается к "первому знакомству"

### 4. Мягкое направление

При 1-2 offtopic подряд бот:
- 💬 Отвечает вежливо
- 🎯 Мягко напоминает о поиске работы
- 😊 Остаётся дружелюбным

Пример:
```
Пользователь: "Как погода?"
Бот: "Давай лучше поищем подходящие вакансии! 😊 Какую работу ты ищешь?"
```

## Проверка работы LLM

### 1. Проверьте логи при запуске

```bash
./logs.sh
```

Вы должны увидеть:
```
Groq LLM сервис инициализирован с 4 ключами
LLM Middleware зарегистрирован
```

### 2. Тестирование в боте

Отправьте боту offtopic сообщение:
```
"Какая погода на улице?"
```

Бот должен ответить что-то вроде:
```
"Давай лучше поищем подходящие вакансии! 😊"
```

### 3. Проверка классификации

Логи покажут результаты классификации:
```
LLM классификация для 'Какая погода...': {'is_relevant': False, 'confidence': 0.95, 'category': 'offtopic'}
```

## Без API ключей

Бот работает **без Groq ключей**, но:
- ❌ Нет AI-ассистента
- ❌ Нет классификации offtopic
- ✅ Весь остальной функционал доступен

В логах будет предупреждение:
```
Groq API ключи не найдены, LLM функционал отключён
```

## Безопасность

### ✅ Правильно:

```env
# В .env файле (не коммитится в git)
GROQ_API_KEY_1=gsk_реальный_ключ
```

### ❌ Неправильно:

```python
# НЕ пишите ключи прямо в коде!
api_key = "gsk_ваш_ключ"  # Плохо!
```

### Важно:

1. Файл `.env` добавлен в `.gitignore` ✅
2. Файл `.env.example` содержит только примеры ✅
3. **Никогда не коммитьте** `.env` в git
4. При продаже/передаче бота - удалите `.env` перед передачей

## Альтернативные модели

Вы можете использовать другие модели Groq:

```env
# Более быстрая, но менее точная
GROQ_MODEL=llama-3.1-8b-instant

# Более мощная (рекомендуется, по умолчанию)
GROQ_MODEL=llama-3.3-70b-versatile
```

## Устранение неполадок

### Проблема: "Groq API ключи не найдены"

**Решение:**
1. Проверьте, что `.env` файл существует
2. Убедитесь, что ключи указаны как `GROQ_API_KEY_1`, `GROQ_API_KEY_2` и т.д.
3. Перезапустите бота: `./restart.sh`

### Проблема: "Rate limit exceeded"

**Решение:**
1. Добавьте больше API ключей (до 4)
2. Проверьте лимиты в [консоли Groq](https://console.groq.com)
3. Подождите до следующего дня (лимиты обновляются ежедневно)

### Проблема: "Invalid API key"

**Решение:**
1. Проверьте, что ключ начинается с `gsk_`
2. Пересоздайте ключ в консоли Groq
3. Убедитесь, что нет лишних пробелов в `.env`

## Полезные ссылки

- [Groq Console](https://console.groq.com)
- [Groq Documentation](https://console.groq.com/docs)
- [Supported Models](https://console.groq.com/docs/models)

## Вопросы?

Если что-то не работает:
1. Проверьте логи: `./logs.sh -e` (только ошибки)
2. Убедитесь, что зависимости установлены: `pip install -r requirements.txt`
3. Проверьте статус: `./status.sh`
