# JobiusBot Documentation

Документация для Telegram бота поиска работы на базе HeadHunter API с интеграцией LLM.

## Структура документации

### [API_REFERENCES.md](API_REFERENCES.md)
Полный справочник по всем используемым API:
- HeadHunter API (параметры поиска, примеры запросов)
- Groq API (LLM интеграция)
- Telegram Bot API
- Полезные ссылки на статьи и ресурсы

### [SMART_SEARCH.md](SMART_SEARCH.md)
Подробная документация функции "Умный поиск":
- Концепция и архитектура
- Поддерживаемые параметры
- Примеры использования
- LLM промпты и настройки
- User flow и обработка ошибок
- Метрики и планы развития

## Быстрый старт

### Основные API endpoints

**HeadHunter поиск вакансий:**
```
GET https://api.hh.ru/vacancies
```

**Параметры:**
- `text` - поисковый запрос
- `area` - ID города (1=Москва, 2=СПб)
- `salary` - минимальная зарплата
- `experience` - опыт работы (noExperience, between1And3, between3And6, moreThan6)
- `schedule` - график (remote, flexible, fullDay)
- `employment` - занятость (full, part, project)

### Примеры запросов

```bash
# Простой поиск Python вакансий в Москве
curl "https://api.hh.ru/vacancies?text=Python&area=1"

# Удаленная работа с зарплатой
curl "https://api.hh.ru/vacancies?text=Frontend&schedule=remote&salary=150000"

# Junior позиция с гибким графиком
curl "https://api.hh.ru/vacancies?text=Developer&experience=between1And3&schedule=flexible"
```

## Функционал бота

### Обычный поиск 🔍
- Прямой поиск по ключевым словам
- Парсинг параметров из текста (город, зарплата, опыт)
- Быстрый и простой

**Пример:**
```
Пользователь: Python москва 150000
Бот: → поиск Python в Москве от 150000₽
```

### Умный поиск 🧠
- Запросы в свободной форме
- LLM парсинг через Groq API
- Поддержка всех параметров HH
- Показ распознанных параметров

**Пример:**
```
Пользователь: Хочу удаленную работу python от 150000
Бот: 🔍 Ищу: python
     🕐 График: Удаленная работа
     💰 От 150 000 ₽
     → поиск с этими параметрами
```

### Избранное ⭐
- Сохранение понравившихся вакансий
- Просмотр сохраненных
- Удаление из избранного

### Статистика 📊
- Количество поисков
- История запросов
- Избранные вакансии
- Дата регистрации

### Калькулятор 🔢
- Быстрые вычисления
- Поддержка базовых операций
- Полезно для расчета зарплаты после налогов

## Технологический стек

### Backend
- **Python 3.12+**
- **aiogram 3.x** - Telegram Bot фреймворк
- **aiohttp** - Асинхронные HTTP запросы
- **aiosqlite** - Асинхронная SQLite БД

### LLM
- **Groq API** - LLM инференс
- **llama-3.3-70b-versatile** - Модель для парсинга

### APIs
- **HeadHunter API** - Поиск вакансий
- **Telegram Bot API** - Интерфейс бота

## Архитектура

```
┌─────────────────┐
│  Telegram User  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Bot Handlers  │ ◄─── FSM States
│  basic.py       │
│  search.py      │
│  favorites.py   │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌────────┐ ┌──────────┐
│ HH API │ │ Groq LLM │
└────────┘ └──────────┘
         │
         ▼
    ┌────────┐
    │ SQLite │
    └────────┘
```

## Конфигурация

### Переменные окружения (.env)
```bash
# Telegram
TELEGRAM_BOT_TOKEN=your_token_here

# Groq API (до 4 ключей для ротации)
GROQ_API_KEY_1=gsk_...
GROQ_API_KEY_2=gsk_...
GROQ_API_KEY_3=gsk_...
GROQ_API_KEY_4=gsk_...

# Модель Groq
GROQ_MODEL=llama-3.3-70b-versatile
```

### config.py
```python
MAX_VACANCIES_SHOW = 20  # Вакансий на страницу

# Популярные города (для парсинга)
POPULAR_AREAS = {
    "москва": 1,
    "спб": 2,
    "питер": 2,
    # ...
}

# Уровни опыта
EXPERIENCE_LEVELS = {
    "junior": "between1And3",
    "middle": "between3And6",
    "senior": "moreThan6",
    # ...
}
```

## База данных

### Таблицы

**users** - Пользователи бота
```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    first_name TEXT,
    last_name TEXT,
    search_count INTEGER DEFAULT 0,
    created_at TIMESTAMP,
    last_active TIMESTAMP
);
```

**favorites** - Избранные вакансии
```sql
CREATE TABLE favorites (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    vacancy_id TEXT,
    vacancy_data TEXT,  -- JSON
    created_at TIMESTAMP
);
```

**search_history** - История поисков
```sql
CREATE TABLE search_history (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    search_query TEXT,
    search_params TEXT,  -- JSON (включая smart_search: true)
    results_count INTEGER,
    created_at TIMESTAMP
);
```

## Разработка

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск бота
```bash
python bot.py
```

### Структура проекта
```
JobiusBot/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация
├── database.py            # Работа с БД
├── hh_api.py             # HeadHunter API клиент
├── handlers/              # Обработчики команд
│   ├── basic.py          # Базовые команды
│   ├── search.py         # Поиск вакансий
│   ├── favorites.py      # Избранное
│   └── easter_eggs.py    # Пасхалки
├── middlewares/           # Middleware
│   └── llm_middleware.py # LLM обработка
├── keyboards/             # Клавиатуры
│   ├── reply.py          # Reply кнопки
│   └── inline.py         # Inline кнопки
├── utils/                 # Утилиты
│   ├── llm_service.py    # Groq LLM сервис
│   ├── states.py         # FSM состояния
│   └── search_manager.py # Менеджер сессий поиска
└── docs/                  # Документация
    ├── README.md
    ├── API_REFERENCES.md
    └── SMART_SEARCH.md
```

## Changelog

### v2.0 (2026-01-19) - Умный поиск
✨ **Новое:**
- Кнопка "🧠 Умный поиск" с LLM парсингом
- Интеграция Groq API (Llama 3.3 70B)
- Поддержка schedule и employment в HH API
- FSM состояние для умного поиска
- Показ распознанных параметров

🔧 **Изменения:**
- Упрощен middleware (убрана блокировка offtopic)
- Обычный поиск работает по умолчанию для всех текстовых сообщений

📚 **Документация:**
- Добавлены API_REFERENCES.md
- Добавлены SMART_SEARCH.md
- Обновлен README.md

### v1.0 - Базовый функционал
- Интеграция с HeadHunter API
- Обычный поиск вакансий
- Избранное
- Статистика
- Калькулятор
- Пагинация результатов
- База данных SQLite

## Поддержка

### Известные проблемы
1. **LLM может неправильно распарсить сложные запросы**
   - Решение: Fallback на text=original_query

2. **Город не находится в POPULAR_AREAS**
   - Решение: Поиск без фильтра по городу

3. **Rate limiting HH API**
   - Решение: Обработка 429 ошибок, retry

### Вопросы и обратная связь
- GitHub Issues: (добавить ссылку)
- Telegram: @your_contact

## Лицензия

MIT License

## Авторы

Создано с использованием:
- Claude Code (Anthropic)
- HeadHunter API
- Groq API

---

**Версия документации:** 2.0
**Последнее обновление:** 2026-01-19
