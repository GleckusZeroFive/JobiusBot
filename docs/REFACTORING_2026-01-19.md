# Рефакторинг проекта JobiusBot

## Дата: 2026-01-19

## Цели
1. Навести порядок в проекте (удалить мусор, структурировать файлы)
2. Упростить UI (убрать лишние кнопки)
3. Объединить умный и обычный поиск с fallback механизмом
4. Сделать базовые функции доступными даже при недоступности LLM

---

## Что сделано

### 1. Чистка проекта

**Удалено:**
- `bot_old.py` — старая версия бота (14KB)
- `test_groq.py`, `test_offtopic.py` — тестовые скрипты
- `.github_ready.txt`, `task.txt` — технический мусор

**Структурировано:**
- Создана папка `scripts/` → перемещены все `.sh` скрипты
- Создана папка `tests/` → для будущих тестов
- Вся документация перемещена в `docs/`

**Результат:** Корень проекта стал чистым и понятным.

---

### 2. Оптимизация UI

**Было (6 кнопок):**
```
[🔍 Поиск работы] [⭐ Избранное]
      [🧠 Умный поиск]
  [📊 Статистика] [❓ Помощь]
      [🔢 Калькулятор]
```

**Стало (4 кнопки):**
```
[🔍 Поиск работы] [⭐ Избранное]
[🔢 Калькулятор]  [📊 Статистика]
```

**Изменения:**
- ❌ Убрана кнопка "🧠 Умный поиск" — теперь все поиски умные
- ❌ Убрана кнопка "❓ Помощь" — доступна через команду `/help`

---

### 3. Объединение поисков

**Новая логика:**
1. Пользователь пишет запрос (любой формат)
2. Бот пытается распарсить через **LLM** (умный режим)
3. Если LLM недоступен → **fallback на regex-парсинг** (обычный режим)
4. Поиск всегда работает, пользователь не видит разницы

**Технические детали:**

Файл: `handlers/search.py`

**Новые функции:**
- `try_smart_parse(query)` — пробует LLM-парсинг, возвращает `None` при ошибке
- `fallback_parse(query)` — regex-парсинг, всегда возвращает результат
- `perform_unified_search()` — основная функция, объединяющая оба подхода

**Что понимает умный поиск:**
- Естественный язык: "Хочу удаленку python от 150к"
- Структурированные запросы: "Python junior Москва 200000"
- Смешанные форматы: "Ищу начальную позицию в IT с гибким графиком"

**Fallback на regex:**
- Извлекает числа → зарплата
- Распознает города из `POPULAR_AREAS`
- Распознает уровни из `EXPERIENCE_LEVELS` (junior, middle, senior)
- Остальное → текст поиска

---

### 4. Обновленные обработчики

**Файл: `handlers/basic.py`**

**Удалено:**
- Обработчик кнопки "🧠 Умный поиск" и команды `/smart`
- Обработчик кнопки "❓ Помощь" (оставлена только команда `/help`)

**Обновлено:**
- Приветственное сообщение `/start` — теперь упоминает понимание естественного языка
- Кнопка "🔍 Поиск работы" — обновлены примеры запросов
- Команда `/help` — обновлена справка (убраны упоминания умного поиска как отдельной функции)

**Файл: `handlers/search.py`**

**Изменено:**
- `cmd_search()` теперь использует `perform_unified_search()`
- `handle_text_message()` использует `perform_unified_search()`
- `perform_search()` теперь принимает готовые параметры (area_id, salary, experience)

---

### 5. Обновление клавиатуры

**Файл: `keyboards/reply.py`**

Функция `get_main_menu()` обновлена:
- Убрана кнопка "🧠 Умный поиск"
- Убрана кнопка "❓ Помощь"
- Кнопки переставлены в 2 ряда по 2 кнопки

---

## Преимущества нового подхода

### 1. Простота для пользователя
- Не нужно выбирать между "умным" и "обычным" поиском
- Просто пишешь запрос → бот сам разбирается
- Меньше кнопок → меньше путаницы

### 2. Надежность
- Если LLM недоступен → fallback на regex
- Базовые функции всегда работают
- Пользователь не видит ошибок "Умный поиск недоступен"

### 3. Чистота кода
- Убран дублирующий код
- Единая точка входа для поиска
- Логика разделена на маленькие функции

### 4. Масштабируемость
- Легко добавить новые источники данных для парсинга
- Можно добавить кэширование LLM-запросов
- Fallback можно улучшать независимо от LLM

---

## Технические подробности

### Fallback механизм

```python
async def perform_unified_search(message, user_id, query):
    # 1. Пробуем LLM
    parsed = await try_smart_parse(query)

    if parsed is not None:
        # LLM сработал → показываем параметры → ищем
        await perform_smart_search(message, user_id, parsed)
    else:
        # LLM не сработал → fallback на regex
        parsed = fallback_parse(query)
        await perform_search(message, user_id, parsed['text'],
                           area_id=parsed.get('area_id'),
                           salary=parsed.get('salary'),
                           experience=parsed.get('experience'))
```

### Логирование

Добавлено логирование для отслеживания:
- Когда используется LLM: `"Используем LLM-парсинг для '{query}'"`
- Когда используется fallback: `"LLM недоступен, используем fallback для '{query}'"`
- Ошибки LLM: `"LLM-парсинг не удался: {error}"`

---

## Что осталось на будущее

### Краткосрочные улучшения:
1. **Улучшить калькулятор** — сделать интерфейс удобнее (кнопки с операциями?)
2. **LLM-аналитика** — добавить сравнение вакансий через естественные запросы
3. **Расчеты зарплат** — калькулятор "на руки" с учетом НДФЛ

### Среднесрочные:
1. **Система переменных** — база данных с НДФЛ, МРОТ, взносами (с автообновлением)
2. **Статистика по вакансиям** — средняя/медианная зарплата по результатам поиска
3. **Рекомендательная система** — LLM анализирует вакансии и дает советы

### Долгосрочные:
1. **Умные уведомления** — подписка на новые вакансии по критериям
2. **История откликов** — отслеживание, куда откликался пользователь
3. **Интеграция с резюме** — анализ соответствия вакансии навыкам пользователя

---

## Тестирование

### Проверено:
- ✅ Синтаксис всех измененных файлов
- ✅ Импорты работают корректно
- ✅ Кнопки отображаются правильно

### Нужно протестировать вручную:
- Поиск через LLM (если доступен)
- Поиск через fallback (если LLM отключить)
- Кнопки главного меню
- Команда `/help`
- Калькулятор

---

## Файлы, затронутые изменениями

### Изменено:
1. `handlers/search.py` — объединение поисков, новые функции
2. `handlers/basic.py` — обновлены обработчики кнопок и команд
3. `keyboards/reply.py` — новое главное меню (4 кнопки)

### Создано:
1. `scripts/` — папка для shell скриптов
2. `tests/` — папка для тестов
3. `REFACTORING_2026-01-19.md` — этот документ

### Удалено:
1. `bot_old.py` — старая версия
2. `test_groq.py`, `test_offtopic.py` — тестовые скрипты
3. `.github_ready.txt`, `task.txt` — технический мусор

### Перемещено:
1. Все `.sh` скрипты → `scripts/`
2. Вся документация (кроме README.md) → `docs/`

---

## Заключение

Проект стал:
- **Чище** — убран мусор, структурированы файлы
- **Проще** — меньше кнопок, интуитивный интерфейс
- **Надежнее** — fallback на regex при недоступности LLM
- **Понятнее** — единый поиск вместо двух режимов

Базовая функциональность (поиск вакансий) теперь **всегда доступна**, независимо от состояния LLM сервиса.
