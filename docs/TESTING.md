# Руководство по тестированию

## Ручное тестирование умного поиска

### Базовые тесты

#### Тест 1: Простой запрос с удаленкой
```
Ввод: Хочу удаленную работу python от 150000

Ожидаемый результат:
🔍 Ищу: python
🕐 График: Удаленная работа
💰 От 150 000 ₽
```

#### Тест 2: Junior позиция в городе
```
Ввод: Junior frontend в Москве

Ожидаемый результат:
🔍 Ищу: frontend
📍 Город: Москва
📊 Опыт: Junior (1-3 года)
```

#### Тест 3: Гибкий график для студента
```
Ввод: Хочу работу для студента с гибким графиком

Ожидаемый результат:
🔍 Ищу: студент
📊 Опыт: Без опыта
🕐 График: Гибкий график
```

#### Тест 4: Подработка
```
Ввод: Нужна подработка для студента

Ожидаемый результат:
🔍 Ищу: студент
📊 Опыт: Без опыта
💼 Занятость: Частичная занятость
```

#### Тест 5: Комплексный запрос
```
Ввод: Ищу middle DevOps в Санкт-Петербурге на удаленку от 200000

Ожидаемый результат:
🔍 Ищу: DevOps
📍 Город: Спб
📊 Опыт: Middle (3-6 лет)
🕐 График: Удаленная работа
💰 От 200 000 ₽
```

### Тесты на синонимы

#### Тест 6: Альтернативные названия городов
```
Ввод: Python в питере
Ожидается: area=2 (Санкт-Петербург)

Ввод: Frontend в СПб
Ожидается: area=2 (Санкт-Петербург)
```

#### Тест 7: Синонимы уровня
```
Ввод: Джун разработчик
Ожидается: experience=between1And3

Ввод: Мидл бэкенд
Ожидается: experience=between3And6

Ввод: Сеньор архитектор
Ожидается: experience=moreThan6
```

#### Тест 8: Синонимы графика
```
Ввод: Удаленка
Ожидается: schedule=remote

Ввод: Remote работа
Ожидается: schedule=remote

Ввод: Офис полный день
Ожидается: schedule=fullDay
```

### Граничные случаи

#### Тест 9: Нечеткий запрос
```
Ввод: Хочу работать

Ожидаемый результат:
🔍 Ищу: работать
(без дополнительных параметров)
```

#### Тест 10: Только город
```
Ввод: Работа в Казани

Ожидаемый результат:
🔍 Ищу: работа
📍 Город: Казань
```

#### Тест 11: Только зарплата
```
Ввод: От 300000

Ожидаемый результат:
🔍 Ищу: (оригинальный запрос или базовый термин)
💰 От 300 000 ₽
```

### Негативные тесты

#### Тест 12: Некорректный город
```
Ввод: Python в Нью-Йорке

Ожидаемый результат:
🔍 Ищу: Python
(город не распознан, поиск без фильтра по городу)
```

#### Тест 13: Offtopic запрос
```
Ввод: Привет, как дела?

Ожидаемый результат:
Бот ищет вакансии по запросу "Привет, как дела?" (обычный поиск)
Потому что мы убрали блокировку в middleware
```

#### Тест 14: Математическое выражение
```
Ввод: 2 + 2

Ожидаемый результат:
= 4
(калькулятор срабатывает до умного поиска)
```

## Тестирование обычного поиска

### Базовые запросы

```
Python → поиск Python вакансий
Python москва → Python в Москве
Python москва 150000 → Python в Москве от 150000₽
Python junior москва → Python junior в Москве
Frontend middle спб 200000 → Frontend middle в СПб от 200000₽
```

## Тестирование пагинации

1. Выполнить любой поиск (обычный или умный)
2. Пролистать страницы кнопками "◀️ Назад" и "Вперед ▶️"
3. Проверить что вакансии не повторяются
4. Проверить корректность счетчика страниц

## Тестирование избранного

1. Выполнить поиск
2. Нажать ⭐ под вакансией
3. Проверить что звезда изменилась на ⭐️
4. Перейти в "Избранное" через кнопку
5. Проверить что вакансия сохранена
6. Нажать ❌ для удаления
7. Проверить что вакансия удалена

## Тестирование статистики

1. Выполнить несколько поисков
2. Добавить вакансии в избранное
3. Нажать "📊 Статистика"
4. Проверить корректность:
   - Количества поисков
   - Количества избранных
   - Последних поисков

## API тестирование

### Прямой запрос к HH API

```bash
# Базовый поиск
curl "https://api.hh.ru/vacancies?text=Python&area=1&per_page=5"

# С schedule и employment
curl "https://api.hh.ru/vacancies?text=Frontend&schedule=remote&employment=full&per_page=5"

# Комплексный запрос
curl "https://api.hh.ru/vacancies?text=DevOps&area=2&experience=between3And6&schedule=remote&salary=200000&per_page=5"
```

### Тестирование Groq API

```python
import asyncio
from groq import AsyncGroq

async def test_groq():
    client = AsyncGroq(api_key="your_key")

    response = await client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[
            {
                "role": "system",
                "content": "Parse job search query to JSON with text, area, salary, experience, schedule, employment"
            },
            {
                "role": "user",
                "content": "Хочу удаленную работу python от 150000"
            }
        ],
        temperature=0.2,
        max_tokens=300
    )

    print(response.choices[0].message.content)

asyncio.run(test_groq())
```

## Unit тесты (TODO)

```python
import pytest
from utils.llm_service import GroqService

@pytest.mark.asyncio
async def test_parse_simple_query():
    service = GroqService(api_keys=["test_key"])
    result = await service.parse_smart_search_query("Python удаленно 150000")

    assert "text" in result
    assert "Python" in result["text"]
    assert result.get("schedule") == "remote"
    assert result.get("salary") == 150000

@pytest.mark.asyncio
async def test_parse_with_city():
    service = GroqService(api_keys=["test_key"])
    result = await service.parse_smart_search_query("Frontend в Москве")

    assert "text" in result
    assert result.get("area") == "москва"

@pytest.mark.asyncio
async def test_parse_with_experience():
    service = GroqService(api_keys=["test_key"])
    result = await service.parse_smart_search_query("Junior разработчик")

    assert "text" in result
    assert result.get("experience") == "between1And3"
```

## Integration тесты (TODO)

```python
@pytest.mark.asyncio
async def test_smart_search_flow():
    # 1. Нажать кнопку умного поиска
    # 2. Ввести запрос
    # 3. Проверить парсинг LLM
    # 4. Проверить запрос к HH
    # 5. Проверить результаты
    pass

@pytest.mark.asyncio
async def test_normal_search_flow():
    # 1. Ввести текст напрямую
    # 2. Проверить обычный парсинг
    # 3. Проверить запрос к HH
    # 4. Проверить результаты
    pass
```

## Performance тесты

### Замер времени ответа LLM

```python
import time
import asyncio
from utils.llm_service import groq_service

async def benchmark_llm():
    queries = [
        "Python удаленно 150000",
        "Junior frontend в Москве",
        "Хочу работу для студента",
        "Middle DevOps в СПб от 200000"
    ]

    for query in queries:
        start = time.time()
        result = await groq_service.parse_smart_search_query(query)
        elapsed = time.time() - start

        print(f"Query: {query}")
        print(f"Result: {result}")
        print(f"Time: {elapsed:.2f}s\n")

asyncio.run(benchmark_llm())
```

### Ожидаемые результаты
- LLM парсинг: 0.5-2 секунды
- HH API запрос: 0.3-1 секунда
- Общее время умного поиска: 1-3 секунды

## Checklist перед релизом

### Функциональность
- [ ] Обычный поиск работает
- [ ] Умный поиск работает
- [ ] Избранное работает
- [ ] Статистика работает
- [ ] Калькулятор работает
- [ ] Пагинация работает
- [ ] Все кнопки меню работают

### LLM
- [ ] Groq API подключен
- [ ] Парсинг запросов корректен
- [ ] Обработка ошибок работает
- [ ] Fallback на обычный поиск работает

### HH API
- [ ] Базовый поиск работает
- [ ] Параметры schedule и employment работают
- [ ] Обработка ошибок 429 (rate limit)
- [ ] Обработка пустых результатов

### База данных
- [ ] Таблицы созданы
- [ ] Сохранение истории работает
- [ ] Сохранение избранного работает
- [ ] Статистика корректна

### UI/UX
- [ ] Кнопки отображаются корректно
- [ ] Сообщения читабельны
- [ ] Emoji используются уместно
- [ ] Ошибки понятны пользователю

### Безопасность
- [ ] API ключи в .env
- [ ] .env в .gitignore
- [ ] Нет SQL injection
- [ ] Нет XSS в сообщениях

## Логи для отладки

### Включение debug логов

```python
# bot.py
logging.basicConfig(
    level=logging.DEBUG,  # Изменить с INFO на DEBUG
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

### Важные логи

```
# LLM парсинг
Smart search parsing: 'Хочу удаленную работу python от 150000' -> {'text': 'python', 'schedule': 'remote', 'salary': 150000}

# HH API запрос
Запрос к HH API: https://api.hh.ru/vacancies с параметрами {'text': 'python', 'schedule': 'remote', 'salary': 150000}

# Результат HH
Получено 245 вакансий, показано 20

# Ошибки
Ошибка парсинга smart search: JSONDecodeError
Ошибка при умном поиске: ConnectionError
```

## Мониторинг в продакшене

### Метрики для отслеживания
- Количество умных поисков vs обычных
- Время ответа LLM
- Частота ошибок LLM парсинга
- Rate limit ошибки HH API
- Популярные запросы
- Конверсия в избранное

### Alerts
- LLM недоступен > 5 минут
- HH API rate limit превышен
- База данных недоступна
- Ошибок > 10% от всех запросов
