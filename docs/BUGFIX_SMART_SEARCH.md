# Исправление умного поиска и обработки offtopic

## Дата: 2026-01-19

## Проблема

1. **Умный поиск не работал** - при использовании команды `/smart` бот возвращал ошибку "❌ Умный поиск временно недоступен"
2. **Обработка offtopic не работала** - бот не отвечал на приветствия и не-релевантные сообщения

## Причина

Проблема заключалась в механизме импорта Python. Когда модули импортировали переменную `groq_service` напрямую, они получали ссылку на `None` до инициализации сервиса. Даже после инициализации в `bot.py`, импортированная переменная в других модулях оставалась `None`.

## Решение

### 1. Изменен подход к доступу к сервису

**Файл:** `utils/llm_service.py`

**Было:**
```python
groq_service: Optional[GroqService] = None

def init_groq_service(api_keys: List[str], model: str):
    global groq_service
    groq_service = GroqService(api_keys, model)
```

**Стало:**
```python
_groq_service: Optional[GroqService] = None

def init_groq_service(api_keys: List[str], model: str):
    global _groq_service
    _groq_service = GroqService(api_keys, model)

def get_groq_service() -> Optional[GroqService]:
    """Получить глобальный экземпляр Groq сервиса"""
    return _groq_service
```

### 2. Обновлены импорты

**Файл:** `handlers/search.py`

**Было:**
```python
from utils.llm_service import groq_service

# В обработчике
if groq_service is None:
    ...
```

**Стало:**
```python
from utils.llm_service import get_groq_service

# В обработчике
groq_service = get_groq_service()
if groq_service is None:
    ...
```

### 3. Восстановлен LLM Middleware

**Файл:** `middlewares/llm_middleware.py`

Восстановлена полная функциональность middleware для обработки offtopic сообщений:
- Классификация сообщений (job_search, bot_help, calculator, offtopic)
- Отслеживание количества offtopic сообщений
- Дружелюбные ответы через LLM на не-релевантные запросы
- Система предупреждений при частых offtopic сообщениях
- Обновлен импорт: `from utils.llm_service import get_groq_service`

## Функциональность

### Умный поиск (`/smart`)

1. Пользователь отправляет запрос в свободной форме
2. LLM парсит запрос и извлекает параметры:
   - `text` - ключевые слова
   - `area` - город
   - `salary` - минимальная зарплата
   - `experience` - уровень опыта
   - `schedule` - график работы
   - `employment` - тип занятости
3. Выполняется поиск с распарсенными параметрами

**Примеры:**
- "Хочу удаленную работу python от 150000" → `{text: "python", schedule: "remote", salary: 150000}`
- "Junior frontend в Москве" → `{text: "frontend", area: "москва", experience: "between1And3"}`

### Обработка offtopic

#### Категории сообщений:
- **job_search** - поиск работы (релевантно)
- **bot_help** - вопросы о боте (релевантно)
- **calculator** - математические вычисления (релевантно)
- **offtopic** - всё остальное (не релевантно)

#### Поведение бота:

1. **Первое offtopic сообщение:**
   - LLM генерирует дружелюбный ответ
   - Перенаправляет на поиск работы

2. **3 offtopic сообщения подряд:**
   - Мягкое напоминание о специализации бота
   - Предложение вернуться к поиску работы

3. **3 подряд + 10 всего offtopic сообщений:**
   - Полный сброс сессии
   - Очистка истории диалогов
   - Сброс счетчиков offtopic

#### Примеры ответов:

- "Привет! Как дела?" → "Мои цифровые импульсы во всю перебирают для вас свежие вакансии! 💼 Давайте найдём что-то интересное?"
- "Какая сегодня погода?" → "К сожалению, я не могу узнать погоду 🌤️ Зато могу помочь найти отличную работу!"
- "Расскажи анекдот" → "Я не комик, но хорошо разбираюсь в вакансиях 😊 Может поищем работу?"

## Тестирование

Созданы тестовые скрипты:

### `test_groq.py`
Тестирует базовую работу Groq API:
- Инициализацию сервиса
- Простые запросы
- Парсинг умного поиска

Запуск: `python test_groq.py`

### `test_offtopic.py`
Тестирует классификацию и ответы на offtopic:
- Классификацию 12 типов сообщений
- Генерацию ответов ассистента

Запуск: `python test_offtopic.py`

## Результаты тестирования

✅ Все тесты успешно пройдены:
- Умный поиск работает
- Классификация сообщений работает (11/12 тестов)
- Генерация ответов работает
- API Groq стабильно работает с 4 ключами

## Файлы, затронутые изменениями

1. `utils/llm_service.py` - добавлена функция `get_groq_service()`
2. `handlers/search.py` - обновлен импорт и использование сервиса
3. `middlewares/llm_middleware.py` - восстановлена полная функциональность
4. `test_groq.py` - создан новый файл
5. `test_offtopic.py` - создан новый файл
6. `BUGFIX_SMART_SEARCH.md` - создан новый файл (эта документация)

## Зависимости

Никаких новых зависимостей не добавлено. Используются существующие:
- `groq` - для работы с LLM API
- `aiogram` - для Telegram бота
- `aiosqlite` - для базы данных

## Рекомендации

1. Периодически проверяйте логи бота на наличие ошибок от Groq API
2. Если один из ключей перестанет работать, система автоматически переключится на следующий
3. Для добавления новых ключей - добавьте `GROQ_API_KEY_5`, `GROQ_API_KEY_6` и т.д. в `.env`
